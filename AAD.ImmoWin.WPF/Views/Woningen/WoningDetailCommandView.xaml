<UserControl x:Class="AAD.ImmoWin.WPF.Views.Woningen.WoningDetailCommandView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AAD.ImmoWin.WPF.Views.Woningen"
             xmlns:viewmodels="clr-namespace:AAD.ImmoWin.WPF.ViewModels.Woningen"
             d:DataContext="{d:DesignInstance Type=viewmodels:WoningDetailCommandViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" IsEnabled="{Binding IsEnabled}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="280"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ContentControl Content="{Binding WoningDetailView}" Grid.Row="0"></ContentControl>
        <StackPanel Orientation="Horizontal" Grid.Row="1">
            <Button Margin="5" Command="{Binding SaveCommand}" Height="30">Save</Button>
            <Button Margin="5" Command="{Binding CancelCommand}" Height="30" >Cancel</Button>
        </StackPanel>
    </Grid>
</UserControl>
