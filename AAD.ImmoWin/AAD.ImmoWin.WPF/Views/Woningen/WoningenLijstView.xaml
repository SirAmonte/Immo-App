<UserControl x:Class="AAD.ImmoWin.WPF.Views.Woningen.WoningenLijstView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AAD.ImmoWin.WPF.Views.Woningen"
             xmlns:viewmodels="clr-namespace:AAD.ImmoWin.WPF.ViewModels.Woningen"
             d:DataContext="{d:DesignInstance Type=viewmodels:WoningenLijstViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" IsEnabled="{Binding IsEnabled}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="250"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Margin="5" Text="Lijst Woningen" Grid.Row="0" Background="LightGray" Height="20" VerticalAlignment="Top"/>
        <ListView Margin="5" ItemsSource="{Binding FilteredWoningen}" SelectedItem="{Binding SelectedWoning}" Grid.Row="1" Width="350" HorizontalAlignment="Left">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Path=., StringFormat='{}{0:T}'}" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <StackPanel Orientation="Vertical"  Grid.Row="2">
            <StackPanel Orientation="Horizontal" Grid.Row="2">
                <Button Margin="5" Command="{Binding CreateCommand}" Height="30">Save</Button>
                <Button Margin="5" Command="{Binding EditCommand}" Height="30">Edit</Button>
                <Button Margin="5" Command="{Binding DeleteCommand}" Height="30">Delete</Button>
                <ComboBox ItemsSource="{Binding Filters}" SelectedItem="{Binding SelectedFilter}" Margin="5" Width="100" Height="30"/>
                <StackPanel Orientation="Vertical">
                    <CheckBox Content="Sort by date" IsChecked="{Binding IsNewest, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
                    <CheckBox Content="Sort by value" IsChecked="{Binding IsValuable, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
                </StackPanel>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
