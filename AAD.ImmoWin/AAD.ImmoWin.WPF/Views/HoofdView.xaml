<Window x:Class="AAD.ImmoWin.WPF.Views.HoofdView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AAD.ImmoWin.WPF.Views" 
        xmlns:viewmodels="clr-namespace:AAD.ImmoWin.WPF.ViewModels"
        mc:Ignorable="d"
        Title="{Binding Titel}" Height="450" Width="800">
    <Window.DataContext>
        <viewmodels:HoofdViewModel></viewmodels:HoofdViewModel>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Menu Grid.Row="0">
            <MenuItem Header="Bestand">
                <MenuItem Header="Afsluiten" Command="{Binding CloseAppCommand}"></MenuItem>
                <MenuItem Header="Lege database" Command="{Binding EmptyDBCommand}"></MenuItem>
                <MenuItem Header="Demo data" Command="{Binding DemoDataCommand}"></MenuItem>
            </MenuItem>
            <MenuItem Header="Gegevens">
                <MenuItem Header="Woningen" Command="{Binding OpenWoningenModuleCommand}"></MenuItem>
                <MenuItem Header="Klanten" Command="{Binding OpenKlantenModuleCommand}"></MenuItem>
            </MenuItem>
        </Menu>
        <ContentControl Content="{Binding CurrentView}" Grid.Row="1"/>
    </Grid>
</Window>
